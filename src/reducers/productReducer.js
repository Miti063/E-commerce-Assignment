import ReduxTypes from '../utils/ReduxTypes';

const INITIAL_STATE = {
  product: [],
};

export default (state = INITIAL_STATE, action) => {
  // const {} = ReduxTypes;
  switch (action.type) {
    default:
      return state;
  }
};
